# ExpertRise BDD Framework Configuration
# Override any value via system property: -Dbase.url=https://...

base.url=https://automationexercise.com
api.base.url=http://localhost:3000
practice.url=https://testautomationpractice.blogspot.com

# Options: chrome | firefox | edge | safari
browser=chrome
# Run headless in CI: -Dheadless=true
headless=false

# Options: chromium | firefox | webkit
playwright.browser=chromium

Timeouts (seconds) 
explicit.wait.seconds=15
page.load.timeout.seconds=30
api.timeout.seconds=30

Reporting
extent.report.dir=target/extent-reports
screenshots.dir=target/screenshots
allure.results.dir=target/allure-results

#Environment
# Override: -Denv=staging
env=QA

# Database Connection (DatabaseUtil)
# H2 in-memory is default (no server needed — perfect for CI)
# Switch to MySQL/Postgres by uncommenting and filling the relevant lines below.
db.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
db.username=sa
db.password=

# MySQL example:
# db.url=jdbc:mysql://localhost:3306/testdb?useSSL=false&allowPublicKeyRetrieval=true
# db.username=root
# db.password=yourpassword

# PostgreSQL example:
# db.url=jdbc:postgresql://localhost:5432/testdb
# db.username=postgres
# db.password=yourpassword
